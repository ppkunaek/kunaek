export interface Teacher {
  teacherId: string;
  teacherName: string;
  teacherTel: string;
  subjectTaught: string;
}

export interface Student {
  StudentId: string,
  StudentName: string,
  StudentAge: number,
  StudentTel: string
}